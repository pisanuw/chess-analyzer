# Marathon: multi-user + Google/magic-link auth

- Goal: Convert chess-analyzer from single-user (Kai) to a small allowlisted multi-user app (members: Kai, Nikash, Neeraj; admin: Yusuf) with Google OAuth + magic-link (Resend) login.
- Done when: A logged-in member sees only their own games/report/repertoire/drills/puzzles, can view their own prep sheet + the full shared scouting library, cannot see other members' deep report/repertoire; admin (Yusuf) can manage all users, import, analyse, and edit engine/LLM settings; Google sign-in and magic link both work locally and on the Netlify mirror; `npm test` green with new multi-user isolation tests. Nikash & Neeraj seeded from their scout books.
- Started: 2026-09-10 · Loop: `/loop /marathon <the task text>` (self-paced; each wake schedules the next via ScheduleWakeup) · Branch: `multi-user` (commit per phase, DO NOT push)

## Decisions (do not re-litigate)
- Hosting: Netlify mirror is the multi-user front door (login lives there); the Mac stays the admin/producer that analyses games and publishes per-user data. Netlify + Supabase back the mirror.
- Provisioning: seed Nikash (FIDE 30960967, "Vemparala, Nikash", 466-game book) and Neeraj (FIDE 30958130, "Harish, Neeraj", 647-game book) by analysing a recent subset of their existing scout books (like the scout `promote` step). Kai = FIDE 39904881.
- Prep sheets are effectively GLOBAL: every member sees their own prep sheet + the full scouting library (all ~80 dossiers). So scouting data (scouts/, prepsheets, clash, players map) stays SHARED. Only own-games/report/repertoire/drills/puzzles are per-user PRIVATE. Each member also gets a self prep sheet (opponent-facing projection of their own games).
- Kai treated like any member. Admin (Yusuf) is the only one who imports/analyses/edits engine+LLM settings and manages users.
- Settings split: engine/LLM/host settings stay GLOBAL (admin). player identity/rating/thresholds become PER-USER.
- No new dependencies: hand-roll OAuth2 + magic-link + sessions with node:crypto + fetch (Google/Resend REST), matching existing server/auth.js. No build step, no framework. Frontend talks to backend only via public/api.js.
- Secrets live in .env (local) / Netlify env (hosted), never committed: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI, RESEND_API_KEY, AUTH_FROM_EMAIL, SESSION_SECRET, PUBLIC_URL. Member emails via AUTH_EMAIL_<ID> env or data/users.json (not secret; data/ is the private data repo).
- data/ is gitignored in the main repo (separate data repo). Phase commits (code) will NOT include data/. Do not commit the data repo. Roster identity/FIDE mapping lives in code (server/users.js DEFAULT_USERS); emails come from env/data.
- Conventions: no em dashes; run `npm test` before each commit; update CHANGES.md (newest first) each phase.

## Work breakdown (each = one phase = one commit on `multi-user`)
- [x] Phase 1: User registry foundation (server/users.js + tests). Roster + allowlist, inert (no behavior change yet).
- [x] Phase 2: Per-user ownership of GAMES in store.js. CHOSE an `owner` field on own games in the shared data/games/ dir (NOT per-user directories, NOT a file migration): scout games stay shared, own games filter by owner, legacy own -> DEFAULT_USER ('kai'). listGames(userId) / listAllGames() / '*' sentinel; getGame/deleteGame ownership params (default unscoped); saveGame stamps owner; gameIndexEntry.owner; ownsGame helper; buildReport/buildRepertoire take userId. test/multiuser.test.js. 156 tests pass. Rationale for the field-not-directory choice: store.test.js reads data/games/<id> paths directly, netlify.toml bundles data/games/*.json, and every writeGame test uses data/games/ - moving files would break all of that, and content-hash ids would collide across per-user dirs when two members played each other.
- [x] Phase 2b: Per-user DRILLS + PATTERNS stores. Done. store.js: userDir(id), getDrills/saveDrills(userId) with local data/users/<id>/drills.json + mirror + Supabase key drills:<id>, getForeignDrillStores(userId), getPatternNotes/savePatternNotes(userId), migrateLegacyUserData(), writeJson now mkdirs the dirname. drills.js: userId threaded through EVERY entry point (default DEFAULT_USER). serve.js: migrate then syncAllDrills per member at startup. report.js: getDrills/getForeignDrillStores(userId). Tests: multiuser.test.js drills/patterns isolation, drills.test.js + report.test.js mirror paths moved to data/users/kai/. 157 tests pass.
- [x] Phase 3a: Session + identity backbone. auth.js: SESSION_SECRET-signed `sess` cookie carrying userId; createSessionToken/verifySessionToken/sessionCookie/clearSessionCookie; authActive() (on when SESSION_SECRET or APP_PASSWORD set); authMiddleware accepts a session OR the legacy password (bearer/`auth` cookie = admin during transition), stays SYNC (roster lookup deferred to currentUser); currentUser(req) resolves the acting user; GET /api/auth/me + POST /api/auth/logout (exempt from auth + read-only gates). test/session.test.js (5). 162 tests pass. Data routes NOT yet scoped to req.user.
- [x] Phase 3b: Identity wired through routes. index.js: effectiveUser(req) (member -> self; admin/local -> ?user or DEFAULT_USER) scopes report/report-card/repertoire/games-list/game-view/drills(GET+review/suspend/undo/restore/decoy)/puzzles/patterns/guess/feedback; requireAdmin(req,res) gates settings PUT, import, delete, player, names, analyse, explain, analyse-all, manual prompt+explanation, reexplain, scout import/promote/narrate, players/link, prepsheet, patterns/synthesize. Global -> listAllGames(): resummarizeGames, /api/scout, promoteStatus, analyse-all, players.js syncPlayers, jobs.js queue+resume, subjects.js gamesForSubject. jobs.js syncDrillsForGame -> game owner. puzzles.js buildPuzzles(userId). test/session.test.js scoping+guard cases. 165 tests pass. DEFERRED to Phase 7 (per-viewer): clash.js loadKaiGames still listGames() default kai (opening clash is kai-centric); subjects.js gamesForSubject now pulls ALL members' own games vs a subject into the shared dossier (fine while shared, revisit if we want viewer-scoped).
- [x] Phase 4: Google OAuth (hand-rolled, no library). server/googleauth.js: googleAuthUrl, signed CSRF state (g_state cookie double-submit), exchangeCodeForIdToken (fetch to Google token endpoint), decodeIdToken (no sig check, TLS-trusted), googleLoginToken (aud + email_verified + allowlist -> createSessionToken). Routes GET /api/auth/google + /callback wired in index.js (exempt from gate). /api/auth/me advertises providers.google/magic. test/googleauth.test.js (8). 173 tests pass. Needs GOOGLE_CLIENT_ID/SECRET + SESSION_SECRET + PUBLIC_URL in env; redirect uri = PUBLIC_URL/api/auth/google/callback.
- [x] Phase 5: Magic link via Resend. server/magiclink.js: makeMagicToken/verifyMagicToken (HMAC signed userId+15min exp), magicRequestRoute (allowlist lookup, same response for unknown to prevent enumeration, rateLimit shared with login, Resend REST send with console fallback when no RESEND_API_KEY), magicVerifyRoute (verify -> getUser recheck -> session). Routes wired in index.js (exempt from gate + read-only). auth.js exports rateLimit. test/magiclink.test.js (5). 178 tests pass. Needs RESEND_API_KEY + AUTH_FROM_EMAIL (+ SESSION_SECRET + PUBLIC_URL) to actually email.
- [ ] Phase 6: Frontend login + per-user views + admin gating. Replace password overlay with a login page (Google button + email field). Nav shows who you are + logout. Hide admin-only UI (import/analyse/settings/user mgmt) for members. Report/repertoire/drills/puzzles = current user; Scouting = full library.
- [ ] Phase 7: Self prep sheet + members in the prep list. Build each member's opponent-facing prep sheet from their own games; expose members alongside scouted opponents; keep each member's deep report/repertoire private.
- [ ] Phase 8: Seed Nikash & Neeraj from scout books. Admin action/script to promote+analyse a recent subset of each member's book as their OWN games (owner=member), producing report/repertoire/drills/puzzles. Run a small validating batch; note remaining as admin engine work.
- [ ] Phase 9: Netlify mirror multi-user + publish pipeline. Function supports the new auth + serves per-user data; publish-web.sh/prebuild bundles per-user data; Supabase drill state namespaced by userId; netlify.toml included_files updated.
- [ ] Phase 10: Docs + cleanup. CLAUDE.md, README, BRIEFING, CHANGES, .env.example with the new vars; final full test pass; verify Done-when conditions.

## Constraints / decisions for a cold wake
- Always read this file + `git log --oneline -15` before doing anything. Trust git + this file over memory.
- Work on branch `multi-user`. Commit code only (data/ is gitignored). Never push.
- Keep each phase bounded and green (`npm test`). If a phase is too big for one wake, split it: check off sub-items in the Log and leave a clear "next" note, still committing a green state.
- NEVER import server/serve.js to smoke-test (it calls app.listen AND runs migrateLegacyUserData + per-member syncAllDrills against the REAL data/ if the port is free). Use `node --check <file>` for syntax. Tests use temp DATA_DIR and are safe. (Learned in Phase 2b: a serve.js import migrated real data/; restored it by moving data/users/kai/{drills.json,drills-<host>.json} back to data/ root and rm -rf data/users. No data lost.)
- Member emails are unknown at build time. Login cannot succeed until the admin fills AUTH_EMAIL_KAI / _NIKASH / _NEERAJ / _YUSUF (or data/users.json). This is a required admin input, not a blocker for building.

## Required admin inputs (surface to user, do not block)
- Member login emails (AUTH_EMAIL_<ID> in .env / Netlify env, or data/users.json).
- Google OAuth app: GOOGLE_CLIENT_ID/SECRET + redirect URI (exact steps to be given in Phase 4).
- Resend: RESEND_API_KEY + a verified AUTH_FROM_EMAIL (user said they will add to .env).
- SESSION_SECRET (random) and PUBLIC_URL (the deployed base URL, for OAuth redirect + magic-link URLs).

## Log
- 2026-09-10 — Set up marathon + branch `multi-user`. Did recon (auth/user/data model), asked 4 clarifying questions (answers recorded under Decisions). Phase 1 done: added server/users.js (roster + allowlist, inert) and test/users.test.js. Next: Phase 2 (per-user data scoping in store.js + migration).
- 2026-09-10 — Phase 2 done (games ownership). Chose an `owner` field over per-user dirs / file migration (see Phase 2 note for why). store.js: DEFAULT_USER/ALL_USERS, ownsGame, listGames(userId)+listAllGames, getGame/saveGame/deleteGame userId params, gameIndexEntry.owner. report.js/repertoire.js take userId. test/multiuser.test.js (8). 156 tests pass. Split off Phase 2b (per-user drills+patterns) since drills carry per-user review state and need their own stores + drills.test.js changes. Next: Phase 2b (per-user drills + patterns).
- 2026-09-10 — Phase 2b done (per-user drills + patterns). userId threaded through store.js drill/pattern helpers + all of drills.js + report.js; serve.js migrates legacy files then syncs each member at startup. 157 tests pass. Note: a serve.js import during smoke-testing migrated the REAL data/ (port was free); restored it, no data lost (added a hard "never import serve.js" rule above). Next: Phase 3 (session + identity in auth.js; wire req.user -> userId across routes, incl. the CRITICAL listGames-caller threading noted in the Phase 3 item).
- 2026-09-10 — Phase 3a done (session + identity backbone). auth.js sessions (SESSION_SECRET-signed sess cookie), authMiddleware accepts session or legacy password, currentUser(req), /api/auth/me + /api/auth/logout. index.js registers the two routes. test/session.test.js (5). 162 tests pass. Split Phase 3 into 3a (backbone, done) and 3b (route wiring + admin gating + global listGames threading). Next: Phase 3b.
- 2026-09-10 — Phase 3b done (identity wired through routes). effectiveUser/requireAdmin in index.js scope every per-user view/write and gate management routes; global callers use listAllGames(); puzzles per-member; jobs sync drills to owner. 165 tests pass. User ran /loop manually ("continue") mid-cadence; did 3b in that turn. Next: Phase 4 (Google OAuth). The auth backbone is ready to issue sessions (createSessionToken); Phase 4/5 add the flows that call it. NOTE: needs GOOGLE_CLIENT_ID/SECRET + SESSION_SECRET in .env to actually run (user was editing .env).
- 2026-09-10 — Phase 4 done (Google sign-in). server/googleauth.js hand-rolled OAuth2 code flow; routes wired; /api/auth/me advertises providers. test/googleauth.test.js (8). 173 tests pass. User manually continued again. Next: Phase 5 (magic link via Resend: POST /api/auth/magic/request emails a signed one-time link, GET /api/auth/magic/verify sets a session; allowlist-only, throttled). Reuse the session cookie + createSessionToken; sign the magic token like the g_state/session tokens; send via Resend REST (RESEND_API_KEY + AUTH_FROM_EMAIL), with a dev fallback that logs the link when RESEND_API_KEY is unset.
