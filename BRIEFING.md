# BRIEFING

Current state of chess-analyzer and what to do next. Keep this short and current.

## Status (2026-09-12)

The improvement report (IMPROVEMENT-REPORT.md) is implemented on branch work merged in phases; 239 unit tests, a browser smoke test (`npm run test:ui`), eslint and a prose check (`npm run lint`), all in CI. Shipped since 2026-09-11, newest first (see CHANGES.md for detail): frontend polish (typed moves under every board, a keyboard map per panel, admin "viewing as", charts that redraw on resize, contrast and focus fixes, the Players list as a sortable table, "main lines only" on the clash with the on-board line highlighted, offline drills through a service worker); training science (per-drill ease, confidence before the reveal with a calibration table and a "sure and wrong" list, explain-back on a miss, "Guess their move" on scouted games); a parsed-game cache and memoised report/repertoire so a member's report is computed once per change (plus the pattern library off the index, explain batches capped at 8 with a size log, pattern names folded onto the library spelling, deterministic time pressure, a depth-tolerant eval cache); the post-game prediction check (did the predicted opening hold, who left it, per-opponent hit rate); the Prepare-for-a-game page with upcoming games and a prep deck (line flashcards, punish drills, sparring from prediction leaves); prep sheet v2 (whole-history grounding, per-student key, evidence citations, markdown export and print); deterministic eval-curve tendencies and book habits, time control in books; the colour-cut dossier, prep rounds, and head-to-head; and the fixes round (stale-session 401, import owner and member names, per-game rating, clash per viewer, audit lock, Supabase fetch timeouts, route split into `server/routes/*`, shared helpers in `public/shared.js`).

## Status (2026-09-11)

Multi-user is merged to `main` and pushed (200 tests). The app serves a small allowlist (members Kai, Nikash, Neeraj; admin Yusuf; plus a `visitor` role) rather than only Kai: allowlist login via Google OAuth and magic link (Resend) with signed session cookies; per-user games (own games carry an `owner` field, scout games shared), drills, pattern notes, report, repertoire, and puzzles; members share the scouting library and prep against each other while their deep report and repertoire stay private; `POST /api/users/:id/seed` builds a member's own games from their scout book. Added this session: an admin-only, producer-only Admin page (`#/admin`) that writes visitors and players to `data/users.json` at runtime (no env edits; merged by `getUsers`, bundled to the mirror on publish); an activity log (`server/audit.js`, `#/log`) of sign-ins (with client IP) and admin mutations; a public "Request access" form on the sign-in screen that emails the admin (`POST /api/auth/request-access`); the Report page split into collapsible sections with the pre-tournament card shown inline (markdown download removed); a Players box to request a prep sheet by FIDE id; and an Opening Clash Tree/Lichess variation-view toggle (per-browser preference). Key files: `server/{users,auth,googleauth,magiclink,audit,email,profiles}.js`, `public/views/{admin,log,review,report,scout}.js`, `.env.example`.

Remaining to go live (admin, on the Mac): `npm run publish-web` to deploy the multi-user function and bundle `data/users.json` + games (Netlify env vars and any new allowlist entries are inert until this runs); add the Google redirect URI `https://chess-analyzer-app.netlify.app/api/auth/google/callback`; seed Nikash and Neeraj (`POST /api/users/<id>/seed`, needs the engine); sign each member in once to confirm Google and the magic link; rotate the Netlify PAT used this session. Allowlist emails are set on Netlify (`AUTH_EMAIL_*`, `AUTH_VISITOR_EMAILS`); the site id and env API are in the `netlify-mirror-deploy` memory.

Before (2026-09-10): v0.1.x, in daily use on Yusuf's Mac with Kai's real tournament games. Since v0.1.0: robustness pass (engine crash-proofing, job/HTTP write safety, corrupt-file tolerance), restart-resuming queue with live depth progress, family sync via the private chess-analyzer-data repo (drills and settings stay per machine), training upgrades (same-session drill retries, honest grading, sharpener tier, multi-move drills, guess seeding, off-list engine evals, per-category trends, time management, pattern synthesis, repertoire view), opponent scouting (dossiers, punish drills, exploitation explanations, prep sheets, opponents auto-derived from own games), editable player names, and a 110-test suite (`npm test`). Rounds on 2026-09-08: fairness fixes (paired same-depth quick evals, win-probability acceptance), game index cache, hosted CAS writes, see-the-threat drills, play-it-out sparring vs a limited engine, pattern lightning rounds, question-first hints, explanation feedback, pre-tournament card, repertoire transposition merging, time-spent strip on the eval graph. Then the review-driven batch: shared chess-math module, threshold tuning without re-analysis, sparring-engine quick evals, opening eval cache, batched whole-game explanations with CLI retry, drill think-time/undo/suspend/category rounds, per-machine review-history sync (drills-<host>.json mirrors merged in the report with recognition-speed medians), quiet-position decoys and opening-prep drills, one-click re-explain of unhelpful explanations, mirror polling stopped and PWA install, category-appropriate drill protocols (play-outs for conversion/defence/endgame-technique, deeper calculation lines, timed mode, pattern rules on a miss, today line), and whole-game guess-the-move. Latest 2026-09-08: a Puzzles tab (free-solve counterpart to Drills, three switchable sources: decisive tactics across all games both sides, own critical moments, own missed tactics), and a publish fix that removed `import.meta` from the bundled server graph so the Netlify CJS function no longer warns (the local entry point is now `server/serve.js`; `server/index.js` is a pure app module). Latest 2026-09-09: opponent scouting from a large per-player export (a "book" tier). A metadb/ChessBase-style PGN whose filename carries the FIDE id imports into a compact `data/scouts/<fideId>.json` (SAN openings, headers, full PGN, no engine work), yielding an instant recency- and rating-weighted dossier (current strength, rating trend, repertoire by colour with share/score) over the opponent's whole history; a promote step sends only the recent, on-strength subset (default 30, within 3 years and ~200 Elo of current) into the existing scout analysis pipeline. Verified against the real 739-game Harish Neeraj export (647 standard games booked, 90 Chess960 skipped). Also 2026-09-09: opponents now key on FIDE id via a synced `data/players.json` (learned from PGN tags and book filenames), an opt-in FIDE-id lookup against ratings.fide.com (confirm each match, never auto-scraped at import), FIDE ids shown in the Scouting view, and the first real dossier runs (Harish Neeraj 30958130, Vemparala Nikash 30960967) promoted and analysed on the 22-host VPN engine pool; the promote default is now 50 recent games, and a bulk pass linked ~80 opponents' FIDE ids. Latest 2026-09-10: the Opening Clash feature shipped (next-step 2, see PLAN-opening-clash.md and CHANGES). It crosses Kai's openings with a scouted opponent's book into an alternating, branching, transposition-merged prediction tree (`server/clash.js`, `GET /api/scout/book/:fideId/clash`), marking per side where prep runs out. The opponent index is parsed once as a background `clash` job and cached (`ensureClashIndex`, `data/clash.json`); the tree assembles cheaply per request. Built out in four phases plus polish: data-only core, interactive read-only board (sticky, with a moves-played list), optional engine-grounded leaf extension (`?extend=1`, home machine only), optional coach narration (`clash/narrate`, home machine only). The build button was dropped for auto-load-on-open with a startup pre-build. It works on the read-only web mirror: the pre-built index and narration are bundled via netlify `included_files` and `scripts/prebuild-clashes.js` (run in `publish-web.sh`), and the mirror assembles the tree in pure JS with no engine/CLI/parse. Scouting page sections are now collapsible `<details>` accordions.

## Purpose

Help a FIDE ~2000 player (Yusuf's son) improve: the engine finds the critical moments, the model explains them and classifies the error, and the classifications accumulate into a weakness profile plus drills from his own games. See README.md for the feature list.

## Decisions

- Local app, `claude` CLI for explanations (no API key, no per-token cost). Frontend kept static so a hosted Netlify build with Stockfish WASM plus an API-key client can be added later without rewriting views.
- Vanilla JS, no build step. chessground for the board, chess.js for rules, hand-rolled SVG charts.
- Error categories (fixed enum in `server/prompts.js`): tactics-allowed, tactics-missed, calculation, positional, opening, endgame-technique, conversion, defence. Time pressure is a separate boolean, not a category.
- Pattern names are free text but the prompt lists the player's existing pattern names so the model reuses them; the report aggregates by normalised name.
- Critical moment threshold default 12 win-probability points (between inaccuracy and mistake). Drill threshold 20.
- Puzzles are derived on demand from the analysed game files (`server/puzzles.js`, `GET /api/puzzles`), with no store and no sync step: they update the instant analysis finishes and need no LLM. Drills, by contrast, are a persisted per-machine store synced after analysis and again after explanation. Keep puzzles stateless (that is what keeps them working on the read-only mirror).
- Local server entry point is `server/serve.js` (owns `listen` and boot housekeeping). `server/index.js` only builds and exports `app`, so tests and the Netlify function can import it without a listener or `import.meta`. Do not add `import.meta` to anything in the function's import graph: the esbuild CJS bundle leaves it empty and warns.
- Opponents key on FIDE id, not name (`data/players.json`, synced small reference data). Ids come free from PGN tags (`WhiteFideId`/`BlackFideId`) and book-export filenames; anything else is resolved by an explicit, confirm-each-match FIDE lookup (`server/fide.js`, ratings.fide.com), never an automatic scrape at import (keeps the local-first principle: only the claude CLI and this deliberate lookup leave the machine).
- Multi-user roster is admin-manageable at runtime: the Admin page writes visitors (`v_<email>`) and players (`p_<email>`) to `data/users.json`, merged into the roster by `getUsers` and bundled to the mirror at publish; built-in members and env visitors (`AUTH_VISITOR_EMAILS`) stay env/code. Activity log lives in `server/audit.js` (Supabase `audit` key on the mirror, `data/audit.json` locally), so sign-ins land on the mirror's shared log while producer-side admin actions land on the Mac's local file. Unauthorized users request access from the sign-in screen (`POST /api/auth/request-access`, public, emails `adminEmail()`). The Opening Clash variation display is a per-browser preference (Tree or Lichess-style), toggled on the clash card.
- Opening Clash is a position-keyed DAG (transpositions merge on the same 3-field posKey used by repertoire.js/scoutbook.js). Kai's side comes from his analysed games (no re-parse); the opponent's side needs parsing their full book PGNs (the book keeps only 10 SAN plies), weighted with scoutDossier's exact recency scheme so the shares reconcile with the prep sheet. Every prompt stays engine-grounded: the model narrates given lines but never picks or evaluates a move. The web mirror serves it by bundling the pre-built index (`data/clash.json`) and narration (`data/clashnotes.json`) and assembling in pure JS; engine extension and narration are home-machine only. `data/clash.json` and `data/clashnotes.json` are per-machine derived caches (gitignored via `ensureDataIgnores`), bundled to the mirror by netlify `included_files` at deploy, not synced through the data repo.

## Known limitations

- One engine process per role (analysis, sparring/quick evals), one job at a time. Fine for a few games per week.
- Phase classification is a material/move-number heuristic (`phaseOf` in analyze.js).
- Drill correctness is "engine best or within 3 win-probability points"; no tablebase check for endgames (play-out drills soften this for conversion/defence).
- The manual LLM flow works but is clunky (copy prompt, paste JSON), and manual mode has no batch or re-explain.
- No lichess/chess.com import yet (public APIs, no auth needed; was descoped for v1). Opponent games now come from FIDE-named per-player exports (the scout book tier); online-username import is still open.
- Scout books: FIDE id comes from the export filename (games carry no FIDE tags), and the subject's side is found by exact name match, so an opponent whose name is spelled differently across files needs an alias. The book's PGN blob is stored per opponent, so `data/scouts/` grows (now excluded from the data repo via `ensureDataIgnores`). The 739-game parse is synchronous (~5s) and blocks the event loop for that one import.
- No unlink/replace for a FIDE-id link: re-linking a name to a second id makes it ambiguous (`lookupFideId` returns null), so correcting a wrong link means editing `data/players.json` by hand. Three homonym opponents (Liu Austin, Xiong Michael, Kwiatkowski Maciej) are unlinked pending a manual pick.
- Decoy selection is heuristic (judgment best, 10+ win-prob spread in the stored lines, ply > 10); tune ratios and criteria from use.

## Next steps, in order

**First: deploy the multi-user mirror** (the code is merged to `main` and pushed): run `npm run publish-web` on the Mac, add the Google redirect URI, seed Nikash and Neeraj (`POST /api/users/<id>/seed`, needs the engine), sign each member in once to confirm Google and the magic link, and rotate the Netlify PAT used this session. See Status above for the full list.

1. Tune thresholds and prompt wording from what Kai finds useful in real use (threshold changes now re-score stored games in place, so this is cheap).
2. DONE (2026-09-10): Repertoire clash / predicted opening (the Opening Clash feature above; PLAN-opening-clash.md is the spec). Remaining polish to consider from real use: tune the branch/depth/min-support caps (`CLASH_DEFAULTS` in `server/clash.js`); optionally build the clash from an opponent's analysed scout games when they have no FIDE book; preserve accordion open-state across a dossier refresh; consider the same accordion treatment on the Drills page (the Report page is now collapsible sections).
3. FIDE-link management: a UI to unlink/replace a wrong id, and finish the 3 held-back homonyms (Liu Austin, Xiong Michael, Kwiatkowski Maciej) by picking the right FIDE record.
4. Lichess and chess.com username import (both public APIs return PGN with clocks).
5. DONE (2026-09-12): per-drill ease scales the ladder from grades, speed, and confidence (`nextEase` in `server/drills.js`). Remaining: tune `EASE_*`, `FAST_MS`, `SLOW_MS` from the accumulating review history, and consider fitting them per member rather than by constant.
6. Tablebase check for 7-man-or-fewer drill positions so endgame correctness means "keeps the theoretical result", not "within 30cp" (needs the lichess tablebase API or local syzygy files; weigh against the everything-runs-locally principle).
7. DONE (2026-09-11): `GET /api/scout/:subject/card` (markdown with evidence footnotes), plus "Copy as markdown" and print on the sheet card.
9. From the improvement report, still open: move the game view's and drills page's play-out code onto `public/playout.js` with one shared `gradeGuess`; retire the manual LLM mode from the UI; use the book's ECO plus the deviation ply in `phaseOf`; schedule the removal of the legacy password login. (Offline drill reviews are now queued and replayed; see CHANGES.md.)
8. Hosted build: `public/` unchanged, `api.js` swapped for a browser backend (Stockfish WASM worker plus Anthropic API client with a user-pasted key). Ask before adding anything that uses an API key. The opening eval cache is the natural seed data for the WASM worker.
